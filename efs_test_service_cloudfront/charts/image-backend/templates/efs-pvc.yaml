# efs-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-claim           # 이름을 deploy.yaml 에서 호출할 예정
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc # storage class name 일치시키기 
  resources:
    requests:
      storage: 5Gi     